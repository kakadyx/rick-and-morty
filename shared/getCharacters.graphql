query getCharacters ($page: Int) {
    characters(page: $page) {
        results {
            id
            name
            image
            origin {
                name
            }
            gender
            status
        }
        info {
            count
            next
        }
    }
}